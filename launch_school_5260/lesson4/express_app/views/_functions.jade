-
  const total    = items => items.reduce((sum, item) => sum + item.price * item.quantity, 0)
  const format   = price => `$${price.toFixed(2)}`
  const quantity = cart => cart.reduce((sum, item) => sum + item.quantity, 0)