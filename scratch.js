/*
- function keyword spacing
- trailing commas



*/
//============================================================================//

// READ: Object, `hasOwnProperty`, and `Object.getOwnPropertyNames` pages on MDN
// READ: Object, `hasOwnProperty`, and `Object.getOwnPropertyNames` pages on MDN
// READ: Object, `hasOwnProperty`, and `Object.getOwnPropertyNames` pages on MDN
// READ: Object, `hasOwnProperty`, and `Object.getOwnPropertyNames` pages on MDN
// READ: Object, `hasOwnProperty`, and `Object.getOwnPropertyNames` pages on MDN
